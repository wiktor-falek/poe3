class Room {
  name: string;
  ilvl: number;
  constructor(name: string, ilvl: number) {
    this.name = name; 
    this.ilvl = ilvl;
  }

  init(): void {
    // 1. generate monsters
    // 2. do stuff?
    // 3. profit
  }
}

export default Room;